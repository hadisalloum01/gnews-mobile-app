# GNews Mobile App

A simple React Native mobile application using the GNews API.

## Setup
1. Install Node.js
2. Run `npm install -g expo-cli`
3. Install dependencies: `npm install`
4. Add your GNews API key in `api/gnews.js`
5. Run: `npx expo start`
